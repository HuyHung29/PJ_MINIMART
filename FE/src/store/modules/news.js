import { useToast } from "vue-toastification";

const toast = useToast();

const state = {
	cart: [],
};

const getters = {};

const actions = {};

const mutations = {};

export default {
	namespaced: true,
	state,
	getters,
	actions,
	mutations,
};
